Implementation:
The timestamp is only accurate to the second, and the pieces of news are keyed to a timestamp. So if messages are entered too quickly (within the same second), the newest message will overwrite the others written during that second. Everything else is implemented correctly.

Time: ~3 hours